<div class="flex items-start space-x-3" id="comment_<%= comment.id %>">
  <div class="w-8 h-8 rounded-full bg-gray-600 flex-shrink-0 overflow-hidden flex items-center justify-center">
    <% 
      commenter_profile = nil
      if comment.user.club?
        commenter_profile = comment.user.club_profile
      elsif comment.user.player?
        commenter_profile = comment.user.player_profile
      elsif comment.user.coach?
        commenter_profile = comment.user.coach_profile
      elsif comment.user.board?
        commenter_profile = comment.user.board_profile
      elsif comment.user.admin?
        commenter_profile = comment.user.admin_profile
      elsif comment.user.user?
        commenter_profile = comment.user.user_profile
      end
    %>
    
    <% if commenter_profile && commenter_profile.profile_picture.attached? %>
      <%= image_tag url_for(commenter_profile.profile_picture), class: "w-full h-full object-cover" %>
    <% else %>
      <%= image_tag "default_pp.jpg", class: "w-full h-full object-cover" %>
    <% end %>
  </div>
  
  <div class="bg-gray-700 rounded-lg pt-2 pb-3 px-3 flex-1">
    <div class="flex justify-between items-center mb-1 relative">
      <div class="flex items-center space-x-2">
        <p class="text-sm font-semibold text-white mt-0 leading-none">
          <% if comment.user.club? %>
            <%= link_to club_profile_path(comment.user.club_profile), 
              data: { turbo_frame: "_top" }, 
              class: "hover:underline" do %>
              <%= comment.user.club_profile.name %>
            <% end %>
          <% elsif comment.user.player? %>
            <%= link_to player_profile_path(comment.user.player_profile), 
              data: { turbo_frame: "_top" }, 
              class: "hover:underline" do %>
              <%= comment.user.player_profile.name %>
            <% end %>
          <% elsif comment.user.coach? %>
            <%= link_to coach_profile_path(comment.user.coach_profile), 
              data: { turbo_frame: "_top" }, 
              class: "hover:underline" do %>
              <%= comment.user.coach_profile.name %>
            <% end %>
          <% elsif comment.user.board? %>
            <%= link_to board_profile_path(comment.user.board_profile), 
              data: { turbo_frame: "_top" }, 
              class: "hover:underline" do %>
              <%= comment.user.board_profile.name %>
            <% end %>
          <% elsif comment.user.admin? %>
            <%= link_to admin_profile_path(comment.user.admin_profile), 
              data: { turbo_frame: "_top" }, 
              class: "hover:underline" do %>
              <%= comment.user.admin_profile.name %>
            <% end %>
          <% elsif comment.user.user? %>
            <%= link_to user_profile_path(comment.user.user_profile), 
              data: { turbo_frame: "_top" }, 
              class: "hover:underline" do %>
              <%= comment.user.user_profile.name %>
            <% end %>
          <% else %>
            <%= comment.user.email %>
          <% end %>
        </p>
        <span class="text-xs text-gray-400 leading-none"><%= time_ago_in_words(comment.created_at) %> atrás</span>
      </div>

      <% if comment.user == current_user or current_user.admin? %>
        <div data-modal-target="modal-apagar-comment-<%= comment.id %>" data-modal-toggle="modal-apagar-comment-<%= comment.id %>" class="flex items-center justify-center w-9 h-9 text-gray-400 hover:text-red-500 hover:bg-gray-600 rounded-full transition clickable" > 
          <i data-feather="trash-2" class="w-4 h-4"></i>
        </div>
      <% else %>
        <div class="flex items-center justify-center w-9 h-9 text-gray-400 hover:text-red-500 hover:bg-gray-600 rounded-full transition clickable" > 
        </div>
      <% end %>
    </div>
    <p class="text-sm text-gray-300"><%= comment.content %></p>
  </div>
</div>


<div id="modal-apagar-comment-<%= comment.id %>" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50">
    <div class="bg-gray-800 rounded-lg p-6 max-w-md w-full mx-4 shadow-2xl border border-gray-700 transform transition-all">
        <div class="flex items-center justify-center w-12 h-12 mx-auto mb-4 bg-red-900 rounded-full">
            <svg class="w-6 h-6 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
            </svg>
        </div>
        <h3 class="text-xl font-semibold text-white text-center mb-2">Confirmar ação</h3>
        <p class="text-gray-300 text-center mb-6"> Tem a certeza que pretende apagar este comentário? Esta ação é irreversível! </b>  </p>
        <div class="flex gap-3">
            <button data-modal-hide="modal-apagar-comment-<%= comment.id %>" class="flex-1 px-4 py-2 bg-gray-700 hover:bg-gray-600 text-white rounded-lg transition-colors font-medium">
                Cancelar
            </button>
            <%= button_to post_post_comment_path(comment.post, comment), 
                method: :delete, 
                data: { 
                  turbo_frame: "_top", 
                  modal_hide: "modal-apagar-comment-#{comment.id}"
                },
                class: "flex-1 px-4 py-2 bg-red-600 hover:bg-red-700 text-white rounded-lg transition-colors font-medium" do %>
                Apagar Comentário
            <% end %>
        </div>
    </div>
</div>