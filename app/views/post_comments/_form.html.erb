<%= form_with model: [post, comment], 
    class: "flex items-center space-x-2 w-full" do |f| %>
  
  <div class="flex-1 relative flex items-center">
    <%= f.text_field :content, 
        placeholder: "Escreve um comentário...", 
        class: "w-full bg-gray-700 rounded-lg px-4 py-3 text-white focus:outline-none focus:ring-2 focus:ring-blue-500",
        autocomplete: "off" %>
  </div>
  
  <%= f.submit "Publicar", 
      class: "text-blue-500 hover:text-blue-400 font-semibold px-3 py-2 cursor-pointer" %>
<% end %>